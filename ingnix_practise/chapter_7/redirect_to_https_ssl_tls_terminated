# Problem - You need to redirect to HTTPs, however, you've terminated SSL/TLS at a layer before NGINX.

server {
	listen 80 default_server;
	list [::]:80 default_server;
	server_name _;
	if ($http_x_forward_proco = 'http') {
		return 301 https://$host$request_url;
	}
}
